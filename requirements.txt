# -- Frozen numeric stack ---------------------------------------------------
# The serving contract (model v0001) is bit-exact ONLY on these versions;
# pandas rolling internals, polars kernels, and numpy scalar semantics all
# feed the pinned parity gates. src/engine/environment.py:VALIDATED_STACK
# mirrors the serving-critical subset and tests/engine/test_environment.py
# enforces that the two never disagree. Upgrading is a deliberate act:
# bump pins + manifest together, re-run the full suite and
# scripts/validate_engine_replay.py, and treat numeric drift as a
# model-retrain event (docs/PRODUCTION.md §7).
numpy==1.26.4
pandas==2.2.3
polars==1.39.3
pyarrow==19.0.1
catboost==1.2.8

# Research/analytics determinism (not on the live serving path, but pinned
# so research results and CI reproduce the validated environment).
scipy==1.17.1
scikit-learn==1.5.2
river==0.21.0

# -- Toolchain ---------------------------------------------------------------
pytest==8.4.2
ruff==0.6.9

# -- Non-numeric utilities (ranged: no effect on serving parity) -------------
optuna>=3.6
plotly>=5.18
kaleido>=0.2
matplotlib>=3.8
seaborn>=0.13
requests>=2.31
tqdm>=4.66
jupyter>=1.0
